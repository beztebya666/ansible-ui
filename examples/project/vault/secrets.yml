---
# Demo secrets consumed by playbook 09.
#
# These are PLAINTEXT for out-of-the-box demoing. In the real world you would:
#     ansible-vault encrypt vault/secrets.yml
# and the shipped .vault_pass (or your own) would decrypt them at run time.
# NEVER commit real credentials — encrypted or not.
vault_admin_user: "admin"
vault_admin_password: "demo-do-not-use-in-prod"
vault_api_token: "tok_demo_3f9a1c7e5b2d4f60a8c1"
